{
	admin off
	auto_https off
}

# Static SPA. Railway sets $PORT.
:{$PORT:8080} {
	root * /usr/share/caddy
	encode gzip

	# Real files win, then directory index, then SPA fallback to index.html.
	try_files {path} {path}/index.html /index.html
	file_server
}
